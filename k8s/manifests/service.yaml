apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service
spec:
  type: ClusterIP
  selector:
    app: springboot-app # 👈 This matches the pod labels from your deployment!
  ports:
  - port: 80
    targetPort: 8081    # 👈 Forwards traffic to your Spring Boot 8081 port
